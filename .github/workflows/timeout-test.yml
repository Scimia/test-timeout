name: test timeout
on:
    workflow_dispatch:
    pull_request:
        types: [opened, synchronize, reopened]
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - run: |
                  echo "Hello world"
                  sleep 5
                  echo "I should run after timeout-minutes is set"
